[user]
	name = Бондарев Никита
	email = nikita.bondarev@simbirsoft.com
[core]
	editor = vim
	pager = delta

[interactive]
    diffFilter = delta --color-only

[include]
    path = /etc/delta/themes.gitconfig 

[delta]
    features = colibri
    side-by-side = true
    navigate = true  # use n and N to move between diff sections
    #dark = true      # or light = true, or omit for auto-detection
    line-numbers = true

[merge]
    conflictstyle = zdiff3
